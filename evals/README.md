# Behavioral regression fixtures

These fixtures define reviewable behavior contracts. They are not universal model benchmarks and do not prove compatibility.

Each fixture contains:

- input;
- required behavior;
- forbidden behavior;
- acceptance evidence;
- remaining unknowns.

A module behavior change must update the relevant fixture. A human or bounded harness trial may record an observed run separately without replacing the fixture contract.
