# Dwi v0.2.3

Release status: approved for publication

Release type: installation-contract patch

Release date: 2026-07-26

## Summary

Dwi v0.2.3 corrects how modules are installed for Codex and Claude Code so the installed artifact preserves the person's decision about when a module may be activated.

The earlier one-file examples were sufficient for skill discovery but incomplete for explicit-only invocation. They also used a relative destination after entering the Dwi source checkout, which could place a module in the source repository instead of the intended project.

Version `v0.2.3` is used because tag `v0.2.2` already existed at a pre-patch commit. That tag remains immutable and is not the installation-contract release.

## Fixed

- Codex installations now include both canonical `SKILL.md` and `agents/openai.yaml` with `allow_implicit_invocation: false`.
- Claude Code installations are rendered from canonical `SKILL.md` with exactly one `disable-model-invocation: true` field.
- The installer refuses to overwrite an existing module directory.
- The installer resolves the prospective destination physically and rejects direct or symlinked paths inside the Dwi source checkout.
- Claude repair backups are stored outside `.claude/skills/`, preventing the affected copy from remaining discoverable during verification.
- English and Vietnamese documentation separate `DWI_ROOT` from `PROJECT_ROOT` and include exact repair and removal paths.
- Localized entry documentation identifies the corrected release without translating operational commands.

## Validation

- Repository contract: PASS on Node.js 20 and 24.
- Installed-artifact contract for all seven modules across Codex and Claude Code: PASS on Node.js 20 and 24.
- Initial release-candidate validation workflow: PASS, run `30175027960`.
- Clean-lineage workflow and release-record validation: PASS, run `30175319793`.
- Direct source-checkout containment rejection: PASS.
- Symlink-alias containment rejection: PASS.
- Clean Codex fresh-session negative and explicit-invocation tests: user-reported PASS on 2026-07-26.
- Clean Claude Code fresh-session negative and explicit-invocation tests: user-reported PASS on 2026-07-26.
- Actionable review threads on PR #5: addressed and resolved.
- Existing conflicting tag detection: PASS; publication stopped rather than rewriting `v0.2.2`.
- Public identity preflight: correctly rejected the unsanitized `main` lineage before any `v0.2.3` tag was pushed.

The runtime observations are bounded. Exact harness versions, operating systems, model versions, and raw transcripts are not attached to the release, so this record does not claim universal compatibility.

## Clean publication lineage

The immutable `v0.2.3` tag is published from a single clean publication commit rather than directly from the unsanitized `main` lineage:

- its tree SHA is exactly equal to the reviewed triggering `main` commit tree;
- its only parent is approved historical boundary `b15a45f00c03c325a12673123f685c32d1ecf8ab`;
- its author and committer are `Trần Thiện Học <hoc@wi.works>`;
- public-release preflight runs with the clean commit checked out on a local branch named `main` and with the local tag already pointing to that commit;
- the tag is pushed only after tree equality, parent, identity, checklist, and preflight checks pass.

The publication commit SHA intentionally differs from the reviewed `main` commit SHA. The content tree is identical. Existing `main` history is not rewritten or presented as sanitized.

## Upgrade guidance

Use the complete installer from the immutable `v0.2.3` tag. Do not install by copying only `SKILL.md` when explicit-only invocation matters.

Existing affected installs may be repaired without changing the canonical module body:

- Codex: add the matching `agents/openai.yaml` beside the installed skill.
- Claude Code: use a local `user-invocable-only` override or reinstall the Claude-rendered artifact.

See [the installation guide](../installation.md) and [the Vietnamese installation guide](../vi/installation.md).

## Compatibility and module impact

- Canonical focused-module bodies changed: no
- Canonical All-in-One body changed: no
- Module SHA-256 values changed: no
- Module catalog membership changed: no
- Native sandbox or approval behavior changed: no
- Installation and activation-policy packaging changed: yes

## Known affected releases and refs

- `v0.1.0`, `v0.2.0`, and `v0.2.1` published the incomplete one-file installation examples.
- Existing tag `v0.2.2` points to pre-patch commit `4fdecbcccb2caf092a145517b2bdcc84e431de27`; it is preserved, not moved, and has no GitHub Release for this patch.
- `v0.2.3` is the corrected installation-contract release.

## Release truth

- Release tag: `v0.2.3`
- Base reviewed repository release: `v0.2.1`
- Canonical module-content baseline: `v0.2.0`
- Included modules: six focused modules and optional All-in-One
- Release approval: Wi explicitly requested publication on 2026-07-26.
- Publication source: a clean commit with the exact reviewed `main` tree and approved public identity
- Existing `main` history: preserved without rewrite
- GitHub Release notes source: this file
