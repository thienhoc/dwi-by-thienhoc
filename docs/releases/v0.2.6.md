# Dwi v0.2.6

Release status: draft, not approved for publication

Release type: Unicode and parser-integrity patch

Release date: 2026-07-26

## Summary

Dwi v0.2.6 addresses three valid review findings reported after `v0.2.5` was published. It further hardens Codex metadata validation without changing module bodies, checksums, installed artifact shapes, native permissions, or the explicit-invocation contract.

## Fixed

- Reject non-ASCII whitespace between a mapping key and its colon, including visually ambiguous no-break spaces.
- Reject YAML-forbidden control characters anywhere in metadata, including inside comments.
- Reject lone UTF-16 surrogate code units in source text and in decoded double-quoted keys or scalar values.
- Preserve valid surrogate pairs and ordinary Unicode content.

## Validation

- Added targeted fixtures for no-break spaces before `interface` and `policy` colons.
- Added control-character fixtures in mapping and scalar comments.
- Added lone high-surrogate and lone low-surrogate escape fixtures.
- Preserved the complete adversarial suite and all seven-module Codex and Claude Code installed-artifact tests.

## Compatibility and module impact

- Canonical module bodies changed: no
- Module SHA-256 values changed: no
- Module catalog membership changed: no
- Codex installed artifact shape changed: no
- Claude Code installed artifact shape changed: no
- Native sandbox or approval behavior changed: no
- Installer validation strictness changed: yes
- Public installation contract changed: no

## Release truth

- Intended release tag: `v0.2.6`
- Base release: `v0.2.5`
- Canonical module-content baseline: `v0.2.0`
- Trigger: three actionable post-release review threads on PR #9
- Existing `v0.2.5` tag and GitHub Release remain immutable
- Publication state: blocked until CI passes, all three threads are answered and resolved, final review is complete, and this record is changed to the exact approved marker
- GitHub Release notes source after approval: this file
