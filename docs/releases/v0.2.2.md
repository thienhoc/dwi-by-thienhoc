# Dwi v0.2.2

Release status: not published

Tag state: existing historical tag only

Recorded date: 2026-07-26

## Summary

The tag `v0.2.2` already existed before the installation-contract patch was ready for publication. It points to commit `4fdecbcccb2caf092a145517b2bdcc84e431de27`, which predates the patch and does not contain the corrected installer, corrected installation guidance, or this release record.

The tag is preserved and will not be moved or rewritten. It must not be treated as the installation-contract release and must not be used as the corrected installation source.

No GitHub Release is published for `v0.2.2`.

## Impact

- Explicit-only installation correction included: no
- Harness-aware installer included: no
- Codex `agents/openai.yaml` installation correction included: no
- Claude Code `disable-model-invocation: true` rendering included: no
- Corrected `DWI_ROOT` and `PROJECT_ROOT` guidance included: no
- Intended installation-contract patch release: advanced to `v0.2.3`

## Immutability decision

Moving `v0.2.2` to a later commit would make the same public tag identify different source over time. Dwi therefore preserves the existing ref and records the conflict instead of rewriting history.

## Guidance

Do not install the corrected patch from `v0.2.2`. Use the next reviewed release identified by the repository's current README and installation guide.

## Evidence

During the gated publication attempt, the fetched local tag resolved to:

```text
4fdecbcccb2caf092a145517b2bdcc84e431de27
```

The approved patch publication commit was different, so the release gate stopped before preflight, tag push, or GitHub Release creation.
